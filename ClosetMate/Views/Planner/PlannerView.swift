
import SwiftUI
import PhotosUI

struct PlannerView: View {
    var body: some View {
        NavigationStack{
            VStack{
                Text("hello")
            }.navigationTitle(Text("Outfit Planner"))
        }
    }
}

#Preview {
    PlannerView()
}
